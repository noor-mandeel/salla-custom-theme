{% extends 'layouts/master.twig' %}

{% block content %}
    <article class="page-content">
        <h1>{{ page.title }}</h1>
        <div class="page-body">
            {{ page.content | raw }}
        </div>
    </article>
{% endblock %}
